# kittokinzzz

This repository contains the portfolio app in `portfolio/portfolio`.

To run the website from the workspace root:

```bash
npm run dev
```

If needed, install dependencies inside the app folder first:

```bash
npm install --prefix portfolio/portfolio
```
